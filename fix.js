const fs = require('fs'); let dynStr = fs.readFileSync('apps/api/test/dynamic-allocation.e2e-spec.ts', 'utf8'); dynStr = dynStr.replace(/counters\[([a-zA-Z0-9]+)\](?!\!)/g, 'counters[' + '' + ']!'); fs.writeFileSync('apps/api/test/dynamic-allocation.e2e-spec.ts', dynStr);
